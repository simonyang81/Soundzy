@Component
export struct Dynamic {
  build() {

    Column() {
      Text('动态')
        .fontColor(Color.Black)
    }
    .width('100%')
    .height('100%')
    .backgroundColor(Color.White)
    
  }
}